<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cs4474.g9.debtledger">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.transaction.CreateTransactionActivity"
            android:label="@string/title_create_transaction" />
        <activity
            android:name=".ui.contacts.select.SelectMultipleContactsActivity"
            android:label="@string/title_select_multiple_contacts" />
        <activity
            android:name=".ui.contacts.select.SelectContactActivity"
            android:label="@string/title_select_contact" />
        <activity
            android:name=".ui.signup.SignupActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>