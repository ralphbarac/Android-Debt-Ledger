<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cs4474.g9.debtledger">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.groups.ViewGroupActivity"
            android:label="@string/title_view_group" />
        <activity
            android:name=".ui.contacts.select.SelectGroupMembersActivity"
            android:label="@string/title_select_group_members" />
        <activity android:name=".ui.groups.CreateEditGroupActivity" />
        <activity
            android:name=".ViewContactActivity"
            android:label="@string/title_view_contact" />
        <activity
            android:name=".ui.transaction.CreateTransactionActivity"
            android:label="@string/title_create_transaction" />
        <activity
            android:name=".ui.contacts.select.SelectWhoOwesActivity"
            android:label="@string/title_select_multiple_contacts" />
        <activity
            android:name=".ui.contacts.select.SelectWhoIsPayingActivity"
            android:label="@string/title_select_contact" />
        <activity
            android:name=".ui.signup.SignupActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
